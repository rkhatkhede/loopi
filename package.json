{
  "name": "loopi-cli",
  "version": "0.0.1",
  "description": "Local Autonomous Improvement Agent — self-improving bot for any code repository",
  "type": "module",
  "bin": {
    "loopi": "dist/cli.js",
    "loopi-cli": "dist/cli.js"
  },
  "files": [
    "dist/",
    "agents/",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "diff": "^5.2.0",
    "picocolors": "^1.1.1",
    "simple-git": "^3.25.0",
    "typescript": "^5.6.0",
    "zod": "^4.4.2"
  },
  "devDependencies": {
    "@types/diff": "^5.2.0",
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "vitest": "^2.0.0"
  }
}
